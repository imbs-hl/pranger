% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_predicted_tree_distance.R
\name{predicted_tree_distance}
\alias{predicted_tree_distance}
\title{Pranger}
\usage{
predicted_tree_distance(tree_index, forest, predictions, init_dist = 0)
}
\arguments{
\item{tree_index}{[\code{integer}] A ranger tree}

\item{forest}{[\code{ranger}] A ranger object}

\item{predictions}{[\code{ranger}] Predictions object from class ranger}

\item{init_dist}{[\code{integer}] Initial distance between in-of-bag nodes}
}
\value{
[\code{matrix}] A matrix of dissimilarities
}
\description{
Uses predicted terminal nodes of a tree to compute
the dissimilarity matrix
}
\examples{
set.seed(1234)
train.idx <- sample(nrow(iris), 2/3 * nrow(iris))
iris.train <- iris[train.idx, ]
iris.test <- iris[-train.idx, ]
rg.iris <- ranger(Species ~ ., data = iris.train)
pred.iris <- predict(rg.iris, data = iris.test, type = "terminalNodes")
 iris_dist <- predicted_tree_distance(
   tree_index = 1,
   forest = rg.iris,
   predictions = pred.iris)
}
\author{
Cesaire J. K. Fouodo
}
